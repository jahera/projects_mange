<!-- Sub-Navbar -->
<% if user_signed_in? && current_user.has_role?('admin')%>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="subNavbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "New User", new_admin_user_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "New project", new_project_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Users", admin_users_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Projects", projects_path, class: "nav-link active" %>
        </li>
      </ul>
    </div>
  </nav>
<% end %>

<!-- Sub-Navbar -->
<% if user_signed_in? && current_user.has_role?('tenant_admin') && @tenant.present? %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="subNavbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'User List', tenant_admin_users_list_path(id: @tenant), class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Project List', tenant_admin_projects_list_path(id: @tenant), class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Task List', tenant_admin_task_list_path(id: @tenant), class: "nav-link active" %>
        </li>
      </ul>
    </div>
  </nav>
<% end %>

<% if user_signed_in? && ( current_user.has_role?('team_member') || current_user.has_role?('project_manager'))%>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="subNavbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Projects", projects_path, class: "nav-link active" %>
        </li>
      </ul>
    </div>
  </nav>
<% end %>